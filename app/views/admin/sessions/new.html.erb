<div class="container" style="margin-top: 50px;">
  <div class="row">
    <div class="col">
      <h2 class="mb-4">管理者ログイン</h2>

      <% if resource.errors.any? %>
        <div class="alert alert-danger">
          <%= resource.errors.count %> 件のエラーが発生しました
          <ul>
            <% resource.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <%= form_with(model: resource, url: session_path(resource_name), local: true, html: { class: "needs-validation", novalidate: true }) do |f| %>
        <table class="table">
          <tr>
            <td style="width: 30%; padding-left: 20px;">メールアドレス</td>
            <td><%= f.email_field :email, class: "form-control", autofocus: true, autocomplete: "email", required: true %></td>
          </tr>
          <tr>
            <td style="width: 30%; padding-left: 20px;">パスワード</td>
            <td><%= f.password_field :password, class: "form-control", autocomplete: "current-password", required: true %></td>
          </tr>
          <% if devise_mapping.rememberable? %>
            <tr>
              <td style="width: 30%; padding-left: 20px;"></td>
              <td>
                <%= f.check_box :remember_me %>
                <%= f.label :remember_me %>
              </td>
            </tr>
          <% end %>
        </table>

        <div class="mt-3">
          <%= f.submit "Log in", class: "btn btn-primary" %>
        </div>
      <% end %>

      <div class="mt-3">
        <%= link_to "Topページ", root_path, class: "btn btn-link" %>
      </div>

      <%= render "admin/shared/links" %>
    </div>
  </div>
</div>
