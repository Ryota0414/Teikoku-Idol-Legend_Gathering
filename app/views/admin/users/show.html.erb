<div>
  <%= image_tag @user.get_profile_image(100,100) %>
  <h3><%= @user.name %></h3>
</div>
  <p><%= link_to "プロフィール編集", edit_admin_user_path(@user) %></p>


<!-- ユーザーの投稿一覧 -->

<% @posts.each do |post| %>
<div>
  <p><%= image_tag @user.get_profile_image(50,50) %><%= post.user.name %></p>
  <p>タイトル : <%= link_to post.title,post %></p>
  <p><%= link_to "#{post.post_comments.count} コメント", post_path(post.id) %></p>
</div>
<% end %>